diff --git a/.env.dev.example b/.env.dev.example
index fbc3c73063..c91c8de37a 100644
--- a/.env.dev.example
+++ b/.env.dev.example
@@ -1,40 +1,105 @@
-# Keys
-# Required key for platform encryption/decryption ops
-# THIS IS A SAMPLE ENCRYPTION KEY AND SHOULD NEVER BE USED FOR PRODUCTION
-ENCRYPTION_KEY=VVHnGZ0w98WLgISK4XSJcagezuG6EWRFTk48KE4Y5Mw=
-
-# JWT
-# Required secrets to sign JWT tokens
-# THIS IS A SAMPLE AUTH_SECRET KEY AND SHOULD NEVER BE USED FOR PRODUCTION
+# =============================================================================
+# Infisical Development Environment Configuration
+# =============================================================================
+# Copy this file to .env and adjust values as needed for your local setup.
+# All ports use environment variable substitution with sensible defaults.
+# =============================================================================
+
+# -----------------------------------------------------------------------------
+# Port Mappings (Host:Container)
+# -----------------------------------------------------------------------------
+# Core Services
+NGINX_PORT=8080
+DB_PORT=5432
+REDIS_PORT=6379
+BACKEND_PORT=4000
+FRONTEND_PORT=5173
+BACKEND_GO_PORT=8082
+
+# ClickHouse
+CLICKHOUSE_HTTP_PORT=8123
+CLICKHOUSE_NATIVE_PORT=9000
+
+# Debug & Monitoring
+BACKEND_OTEL_PORT=9464
+BACKEND_DEBUG_PORT=9229
+
+# Utility Services
+REDIS_COMMANDER_PORT=8085
+BULL_BOARD_PORT=3008
+PGADMIN_PORT=5050
+DB_TEST_PORT=5430
+
+# SMTP / MailHog
+SMTP_PORT=1025
+MAILHOG_UI_PORT=8025
+
+# LDAP (Profile: ldap)
+LDAP_PORT=389
+LDAPS_PORT=636
+PHPLDAPADMIN_PORT=6433
+
+# SSO (Profile: sso)
+KEYCLOAK_PORT=8088
+
+# PingFederate (Profile: pingfed)
+PINGFED_ADMIN_PORT=9999
+PINGFED_RUNTIME_PORT=9031
+
+# Samba AD (Profile: ad)
+SAMBALDAP_PORT=1389
+SAMBALDAPS_PORT=1636
+SAMBA_KERBEROS_PORT=88
+
+# Metrics (Profile: metrics)
+PROMETHEUS_PORT=9090
+GRAFANA_PORT=3005
+
+# OpenTelemetry (Profile: metrics-otel)
+OTEL_PPROF_PORT=1888
+OTEL_PROMETHEUS_PORT=8888
+OTEL_PROMETHEUS_EXPORT_PORT=8889
+OTEL_HEALTH_PORT=13133
+OTEL_GRPC_PORT=4317
+OTEL_HTTP_PORT=4318
+OTEL_ZPAGES_PORT=55679
+
+# -----------------------------------------------------------------------------
+# Required Keys & Secrets
+# -----------------------------------------------------------------------------
+# Key for platform encryption/decryption ops
+ENCRYPTION_KEY=f13dbc92aaaf86fa7cb0ed8ac3265f47
+
+# Secret to sign JWT tokens
 AUTH_SECRET=5lrMXKKWCVocS/uerPsl7V+TX/aaUaI7iDkgl3tSmLE=
 
-# Postgres creds
+# Postgres credentials
 POSTGRES_PASSWORD=infisical
 POSTGRES_USER=infisical
 POSTGRES_DB=infisical
 
-# Required
+# Database connection URI (uses above credentials)
 DB_CONNECTION_URI=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
 
-# Redis
+# Redis connection URL
 REDIS_URL=redis://redis:6379
 
-# Website URL
-# Required
-SITE_URL=http://localhost:8080
-
-# Mail/SMTP
+# Website URL (must match NGINX_PORT)
+SITE_URL=http://localhost:${NGINX_PORT:-8080}
 
-SMTP_HOST=host.docker.internal
+# -----------------------------------------------------------------------------
+# Mail/SMTP Configuration
+# -----------------------------------------------------------------------------
+SMTP_HOST=smtp-server
 SMTP_PORT=1025
-SMTP_FROM_ADDRESS=dev@infisical.local
-SMTP_FROM_NAME=Infisical
-SMTP_REQUIRE_TLS=false
-SMTP_USERNAME=you@example.com
+SMTP_FROM_ADDRESS=test@infisical.com
+SMTP_FROM_NAME=Infisical Dev
+SMTP_USERNAME=
 SMTP_PASSWORD=
 
-# Integration
-# Optional only if integration is used
+# -----------------------------------------------------------------------------
+# Optional Integration Credentials
+# -----------------------------------------------------------------------------
 CLIENT_ID_HEROKU=
 CLIENT_ID_VERCEL=
 CLIENT_ID_NETLIFY=
@@ -151,3 +216,10 @@ DATADOG_HOSTNAME=
 
 # kubernetes
 KUBERNETES_AUTO_FETCH_SERVICE_ACCOUNT_TOKEN=false
+
+# PingFederate dev container (only needed for `make up-dev-pingfed`)
+PING_IDENTITY_DEVOPS_USER=
+PING_IDENTITY_DEVOPS_KEY=
+
+# Local Active Directory dev container (only needed for `make up-dev-ad`)
+SAMBA_ADMIN_PASSWORD=Passw0rd!
diff --git a/docker-compose.dev.yml b/docker-compose.dev.yml
index 72d76b4dc0..cb52b17544 100644
--- a/docker-compose.dev.yml
+++ b/docker-compose.dev.yml
@@ -6,7 +6,7 @@ services:
     image: nginx
     restart: "always"
     ports:
-      - 8080:80
+      - "${NGINX_PORT:-8080}:80"
     volumes:
       - ./nginx/default.dev.conf:/etc/nginx/conf.d/default.conf:ro
     depends_on:
@@ -16,7 +16,7 @@ services:
   db:
     image: postgres:14-alpine
     ports:
-      - "5432:5432"
+      - "${DB_PORT:-5432}:5432"
     volumes:
       - postgres-data:/var/lib/postgresql/data
     environment:
@@ -30,7 +30,7 @@ services:
     environment:
       - ALLOW_EMPTY_PASSWORD=yes
     ports:
-      - 6379:6379
+      - "${REDIS_PORT:-6379}:6379"
     volumes:
       - redis_data:/data
 
@@ -39,8 +39,8 @@ services:
     container_name: infisical-dev-clickhouse
     restart: unless-stopped
     ports:
-      - "8123:8123"
-      - "9000:9000"
+      - "${CLICKHOUSE_HTTP_PORT:-8123}:8123"
+      - "${CLICKHOUSE_NATIVE_PORT:-9000}:9000"
     volumes:
       - clickhouse_data:/var/lib/clickhouse
       - clickhouse_logs:/var/log/clickhouse-server
@@ -63,7 +63,7 @@ services:
     environment:
       - REDIS_HOSTS=local:redis:6379
     ports:
-      - "8085:8081"
+      - "${REDIS_COMMANDER_PORT:-8085}:8081"
 
   bull-board:
     container_name: infisical-dev-bull-board
@@ -77,14 +77,14 @@ services:
       - BULL_VERSION=BULLMQ
       - BULL_PREFIX=bull
     ports:
-      - "3008:3000"
+      - "${BULL_BOARD_PORT:-3008}:3000"
     profiles: [queue]
 
   db-test:
     profiles: ["test"]
     image: postgres:14-alpine
     ports:
-      - "5430:5432"
+      - "${DB_TEST_PORT:-5430}:5432"
     environment:
       POSTGRES_PASSWORD: infisical
       POSTGRES_USER: infisical
@@ -106,64 +106,21 @@ services:
     env_file:
       - .env
     ports:
-      - 4000:4000
-      - 9464:9464 # for OTEL collection of Prometheus metrics
-      - 9229:9229 # For debugger access
+      - "${BACKEND_PORT:-4000}:4000"
+      - "${BACKEND_OTEL_PORT:-9464}:9464"
+      - "${BACKEND_DEBUG_PORT:-9229}:9229"
     environment:
       - NODE_ENV=development
       - DB_CONNECTION_URI=postgres://infisical:infisical@db/infisical?sslmode=disable
       - TELEMETRY_ENABLED=false
-      # Dev-only: lets the backend accept PingFederate's self-signed cert when testing
-      # OIDC locally via `make up-dev-pingfed`. NEVER set this in production.
-      # - NODE_TLS_REJECT_UNAUTHORIZED=0
     volumes:
       - ./backend/src:/app/src
-      - softhsm_tokens:/etc/softhsm2/tokens # SoftHSM tokens are stored in a volume to persist across container restarts
+      - ./backend/package.json:/app/package.json
+      - ./backend/package-lock.json:/app/package-lock.json
+      - ./backend/nodemon.json:/app/nodemon.json
+      - softhsm_tokens:/etc/softhsm2/tokens
     extra_hosts:
       - "host.docker.internal:host-gateway"
-      - "pf.local:host-gateway" # PingFederate dev container — resolves the same on host and inside compose
-    healthcheck:
-      test: ["CMD", "curl", "-f", "http://localhost:4000/api/status"]
-      interval: 30s
-      timeout: 5s
-      retries: 3
-      start_period: 120s
-
-  prometheus:
-    image: prom/prometheus
-    volumes:
-      - ./prometheus.dev.yml:/etc/prometheus/prometheus.yml
-    ports:
-      - "9090:9090"
-    command:
-      - "--config.file=/etc/prometheus/prometheus.yml"
-    profiles: [metrics]
-
-  otel-collector:
-    image: otel/opentelemetry-collector-contrib
-    volumes:
-      - ./otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml
-    ports:
-      - 1888:1888 # pprof extension
-      - 8888:8888 # Prometheus metrics exposed by the Collector
-      - 8889:8889 # Prometheus exporter metrics
-      - 13133:13133 # health_check extension
-      - 4317:4317 # OTLP gRPC receiver
-      - 4318:4318 # OTLP http receiver
-      - 55679:55679 # zpages extension
-    profiles: [metrics-otel]
-
-  grafana:
-    image: grafana/grafana
-    container_name: grafana
-    restart: unless-stopped
-    environment:
-      - GF_LOG_LEVEL=debug
-    ports:
-      - "3005:3000"
-    volumes:
-      - "grafana_storage:/var/lib/grafana"
-    profiles: [metrics]
 
   frontend:
     container_name: infisical-dev-frontend
@@ -173,11 +130,22 @@ services:
     build:
       context: ./frontend
       dockerfile: Dockerfile.dev
+    ports:
+      - "${FRONTEND_PORT:-5173}:5173"
+    environment:
+      - NODE_ENV=development
+      - VITE_DEV_BACKEND_URL=http://backend:4000
     volumes:
-      - ./frontend/src:/app/src/ # mounted whole src to avoid missing reload on new files
+      - ./frontend/src:/app/src
       - ./frontend/public:/app/public
       - ./frontend/index.html:/app/index.html
-    env_file: .env
+      - ./frontend/package.json:/app/package.json
+      - ./frontend/package-lock.json:/app/package-lock.json
+      - ./frontend/vite.config.ts:/app/vite.config.ts
+      - ./frontend/tsconfig.json:/app/tsconfig.json
+      - frontend_node_modules:/app/node_modules
+    entrypoint: ["/app/dev-entrypoint.sh"]
+    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
 
   backend-go:
     container_name: infisical-dev-api-go
@@ -187,46 +155,41 @@ services:
     depends_on:
       db:
         condition: service_started
-      redis:
-        condition: service_started
-      backend:
-        condition: service_healthy
+    env_file:
+      - .env
     ports:
-      - 4040:4040
+      - "${BACKEND_GO_PORT:-8082}:8080"
     volumes:
       - ./backend-go:/app
+      - backend_go_node_modules:/app/node_modules
     environment:
+      - NODE_ENV=development
       - DB_CONNECTION_URI=postgres://infisical:infisical@db/infisical?sslmode=disable
-      - REDIS_URL=redis://redis:6379
-    env_file:
-      - .env
-    profiles: [go]
+    extra_hosts:
+      - "host.docker.internal:host-gateway"
 
   pgadmin:
     image: dpage/pgadmin4
     restart: always
-    volumes:
-      - ./servers.json:/pgadmin4/servers.json
     environment:
       PGADMIN_DEFAULT_EMAIL: admin@example.com
       PGADMIN_DEFAULT_PASSWORD: pass
     ports:
-      - 5050:80
+      - "${PGADMIN_PORT:-5050}:80"
     depends_on:
       - db
 
   smtp-server:
     container_name: infisical-dev-smtp-server
-    image: lytrax/mailhog:latest # https://github.com/mailhog/MailHog/issues/353#issuecomment-821137362
+    image: lytrax/mailhog:latest
     restart: always
     logging:
-      driver: "none" # disable saving logs
+      driver: "none"
     ports:
-      - 1025:1025 # SMTP server
-      - 8025:8025 # Web UI
+      - "${SMTP_PORT:-1025}:1025"
+      - "${MAILHOG_UI_PORT:-8025}:8025"
 
   openldap:
-    # note: more advanced configuration is available
     image: osixia/openldap:1.5.0
     restart: always
     environment:
@@ -234,22 +197,21 @@ services:
       LDAP_DOMAIN: acme.com
       LDAP_ADMIN_PASSWORD: admin
     ports:
-      - 389:389
-      - 636:636
+      - "${LDAP_PORT:-389}:389"
+      - "${LDAPS_PORT:-636}:636"
     volumes:
       - ldap_data:/var/lib/ldap
       - ldap_config:/etc/ldap/slapd.d
     profiles: [ldap]
 
   phpldapadmin:
-    # username: cn=admin,dc=acme,dc=com, pass is admin
     image: osixia/phpldapadmin:latest
     restart: always
     environment:
       - PHPLDAPADMIN_LDAP_HOSTS=openldap
       - PHPLDAPADMIN_HTTPS=false
     ports:
-      - 6433:80
+      - "${PHPLDAPADMIN_PORT:-6433}:80"
     depends_on:
       - openldap
     profiles: [ldap]
@@ -262,17 +224,10 @@ services:
       - KC_BOOTSTRAP_ADMIN_USERNAME=admin
     command: start-dev
     ports:
-      - 8088:8080
+      - "${KEYCLOAK_PORT:-8088}:8080"
     profiles: [sso]
 
   pingfederate:
-    # Requires free Ping Identity DevOps credentials — register at
-    # https://devops.pingidentity.com/how-to/devopsRegistration/
-    # then set PING_IDENTITY_DEVOPS_USER and PING_IDENTITY_DEVOPS_KEY in your .env.
-    # Alternatively, leave both unset to run in Trial Product mode using the
-    # bundled license from pingidentity-server-profiles (expires periodically).
-    # Admin console: https://localhost:9999/pingfederate (administrator / 2FederateM0re)
-    # Runtime endpoint (SAML/OIDC/SCIM): https://localhost:9031
     image: pingidentity/pingfederate:2604
     restart: always
     environment:
@@ -281,12 +236,10 @@ services:
       - PING_IDENTITY_DEVOPS_KEY=${PING_IDENTITY_DEVOPS_KEY}
       - SERVER_PROFILE_URL=https://github.com/pingidentity/pingidentity-server-profiles.git
       - SERVER_PROFILE_PATH=getting-started/pingfederate
-      # Enable Outbound Provisioning runtime for SCIM testing.
-      # Maps to pf.provisioner.mode in run.properties. STANDALONE = single node.
       - PF_PROVISIONER_MODE=STANDALONE
     ports:
-      - 9999:9999 # admin console
-      - 9031:9031 # runtime (SAML/OIDC/SCIM)
+      - "${PINGFED_ADMIN_PORT:-9999}:9999"
+      - "${PINGFED_RUNTIME_PORT:-9031}:9031"
     volumes:
       - pingfederate_data:/opt/out
     extra_hosts:
@@ -294,10 +247,6 @@ services:
     profiles: [pingfed]
 
   samba-ad:
-    # Samba 4 Active Directory Domain Controller for SCIM/SSO testing.
-    # Bootstraps an ACME.LOCAL domain on first start (~60s).
-    # Admin: Administrator / value of SAMBA_ADMIN_PASSWORD (defaults to Passw0rd!).
-    # Reachable from compose network at samba-ad:389; from host at localhost:1389.
     image: nowsci/samba-domain:latest
     container_name: infisical-dev-samba-ad
     hostname: dc1
@@ -310,17 +259,56 @@ services:
       - DOMAINPASS=${SAMBA_ADMIN_PASSWORD:-Passw0rd!}
       - DNSFORWARDER=8.8.8.8
       - HOSTIP=0.0.0.0
-      - INSECURELDAP=true # dev only — allows ldap:// without TLS
+      - INSECURELDAP=true
     ports:
-      - 1389:389 # ldap (mapped off 389 to coexist with the openldap profile)
-      - 1636:636 # ldaps
-      - 88:88 # kerberos
-      - 88:88/udp
+      - "${SAMBALDAP_PORT:-1389}:389"
+      - "${SAMBALDAPS_PORT:-1636}:636"
+      - "${SAMBA_KERBEROS_PORT:-88}:88"
+      - "${SAMBA_KERBEROS_PORT:-88}:88/udp"
     volumes:
       - samba_data:/var/lib/samba
       - samba_etc:/etc/samba/external
     profiles: [ad]
 
+  prometheus:
+    image: prom/prometheus
+    container_name: infisical-dev-prometheus
+    restart: unless-stopped
+    volumes:
+      - ./prometheus.yml:/etc/prometheus/prometheus.yml
+      - softhsm_tokens:/etc/softhsm2/tokens
+    ports:
+      - "${PROMETHEUS_PORT:-9090}:9090"
+    command:
+      - "--config.file=/etc/prometheus/prometheus.yml"
+    profiles: [metrics]
+
+  otel-collector:
+    image: otel/opentelemetry-collector-contrib
+    volumes:
+      - ./otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml
+    ports:
+      - "${OTEL_PPROF_PORT:-1888}:1888"
+      - "${OTEL_PROMETHEUS_PORT:-8888}:8888"
+      - "${OTEL_PROMETHEUS_EXPORT_PORT:-8889}:8889"
+      - "${OTEL_HEALTH_PORT:-13133}:13133"
+      - "${OTEL_GRPC_PORT:-4317}:4317"
+      - "${OTEL_HTTP_PORT:-4318}:4318"
+      - "${OTEL_ZPAGES_PORT:-55679}:55679"
+    profiles: [metrics-otel]
+
+  grafana:
+    image: grafana/grafana
+    container_name: grafana
+    restart: unless-stopped
+    environment:
+      - GF_LOG_LEVEL=debug
+    ports:
+      - "${GRAFANA_PORT:-3005}:3000"
+    volumes:
+      - "grafana_storage:/var/lib/grafana"
+    profiles: [metrics]
+
 volumes:
   postgres-data:
     driver: local
@@ -338,3 +326,5 @@ volumes:
   grafana_storage:
   softhsm_tokens:
     driver: local
+  frontend_node_modules:
+  backend_go_node_modules:
diff --git a/frontend/dev-entrypoint.sh b/frontend/dev-entrypoint.sh
new file mode 100644
index 0000000000..c798cf6b2b
--- /dev/null
+++ b/frontend/dev-entrypoint.sh
@@ -0,0 +1,16 @@
+#!/bin/sh
+
+echo "Starting frontend development environment..."
+
+# Check if node_modules exists and has content
+if [ ! -d "/app/node_modules" ] || [ -z "$(ls -A /app/node_modules 2>/dev/null)" ]; then
+  echo "Installing frontend dependencies..."
+  npm install --ignore-scripts
+  echo "Frontend dependencies installed successfully."
+else
+  echo "Node modules already exist, skipping installation."
+  echo "If you need to reinstall, run: rm -rf /app/node_modules && npm install"
+fi
+
+# Execute the main command
+exec "$@"
